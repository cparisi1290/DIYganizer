<h3>Select a Room</h3>

<%= f.collection_select :room_id, current_user.rooms.uniq, :id, :name, :include_blank => true %> <br>

<h4>Or Create a New Room</h4>

<%= f.fields_for :room do |r| %>
    <%= r.label :name %>
    <%= r.text_field :name %><br>
<% end %><br>
